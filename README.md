# Artist portfolio

Next.js + Tailwind. Project list and detail pages with **grey placeholder blocks** (no CMS, no crawl required).

```bash
npm install
npm run dev
```

Optional: `NEXT_PUBLIC_SITE_TITLE` / `NEXT_PUBLIC_SITE_DESCRIPTION` in `.env.local`.
